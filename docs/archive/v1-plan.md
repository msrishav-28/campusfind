# Archive — V1 plan (superseded)

This file is kept so the same-day V1 lock is not lost. **Implement [../01-plan.md](../01-plan.md) (v1.1), not this.**

v1.1 adds:

- mymind-style photo + hold-to-talk capture
- free vision/language matching (retrieve 3, human confirms)
- campus tenancy (`campus_id`, `/kengeri`)
- explicit v1 / v1.1 / v2 split
- open-source Modal path (SigLIP 2 + Qwen VL)

Original V1 one-liner: a shared campus map where a student pins a lost or found item in twenty seconds, using GPS plus named Kengeri places.

Original V1 defaults that still hold: map is home, no login to post found, GPS snap + drag, 14-day expiry, no 3D in this app, WhatsApp + PWA distribution.
